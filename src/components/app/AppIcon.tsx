import {FontAwesomeIcon, FontAwesomeIconProps} from "@fortawesome/react-fontawesome";
import React from "react";

export default function AppIcon(props: FontAwesomeIconProps) {
    return (
        <>
            <FontAwesomeIcon {...props}/>
        </>
    );
};