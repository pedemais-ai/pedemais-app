import {Metadata} from "next";

export const metadata: Metadata = {
    title: 'Register',
};

export default function Register() {
    return (
        <>
            <p className={"m-3"}>Create account</p>
        </>
    )
}